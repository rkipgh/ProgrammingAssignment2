makeVector <- function(x = numeric()) {
m <- NULL
set <- function(y) {
x <<- y
m <<- NULL
}
get <- function() x
setmean <- function(mean) m <<- mean
getmean <- function() m
list(set = set, get = get,
setmean = setmean,
getmean = getmean)
}
makeVector
makeVector(5)
a=1:10
makeVector(a)
print(makeVector)
print(makeVector())
print(makeVector(a))
makeVector
m <- NULL
m
set <- function(y) {
x <<- y
m <<- NULL
}
set
get <- function() x
setmean <- function(mean) m <<- mean
getmean <- function() m
list(set = set, get = get,
setmean = setmean,
getmean = getmean)
get
setmean
getmean
?matrix
ls()
x=matrix(data=1:9,nrow=1,ncol=1,byrow=T)
x
matrix(data=1:9,nrow=1,ncol=1,byrow=T)
matrix(data=1:9,nrow=3,ncol=3,byrow=T)
x=matrix(data=1:9,nrow=3,ncol=3,byrow=T)
x
solve(x)
x=matrix(data=1:4,nrow=2,ncol=2,byrow=T)
solve(x)
x
makeCacheMatrix <- function(x = matrix()) {
i <- NULL
set <- function(y) {
x <<- y
i <<- NULL
}
get <- function() x
setinverse <- function(solve) i <<- inverse
getinverse <- function() i
list(set = set, get = get,
setinverse = setinverse,
getinverse = getinverse)
}
makeCacheMatrix
x
makeCacheMatrix(x)
